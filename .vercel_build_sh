#!/usr/bin/env bash

# Install Composer secara lokal
curl -sS https://getcomposer.org/installer | php

# Install dependencies Laravel
php composer.phar install --no-dev --optimize-autoloader

# Build frontend React + Vite
npm install
npm run build
